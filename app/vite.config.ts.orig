import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";

export default defineConfig({
  plugins: [react()],

  // Use relative asset paths so the application works through
  // code-server's /proxy/5173/ path and on Cloudflare Pages.
  base: "./",

  server: {
    host: "0.0.0.0",
    port: 5173,
    strictPort: true,
    allowedHosts: ["code.marketplaceai.io"],
    hmr: {
      protocol: "wss",
      host: "code.marketplaceai.io",
      clientPort: 443,
    },
  },

  preview: {
    host: "0.0.0.0",
    port: 4173,
    strictPort: true,
    allowedHosts: ["code.marketplaceai.io"],
  },
});
